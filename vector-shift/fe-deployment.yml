apiVersion: apps/v1
kind: Deployment
metadata:
  name: vs-fe
  labels:
    app: vs-fe
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: vs-fe
  template:
    metadata:
      labels:
        app: vs-fe
    spec:
      containers:
        - name: vs-fe
          image: sujiththirumalaisamy/vector-shift-fe:66f3dfb3912ebeaf739dde3f10b6c22038a28dbd
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 10
